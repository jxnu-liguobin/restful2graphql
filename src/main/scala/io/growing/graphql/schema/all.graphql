# create auto, do not edit it
type Query {
    post(postId: Int!): [Post]
    posts: [Post]
}

type Mutation {
    createPost(post:InputPost!): Post
    updatePost(postId: Int!, post:InputPost!): Post
}

type Author  {
    id: Int!
    firstName: String
    lastName: String
    posts: [Post]
}

type Post  {
    id: Int!
    title: String
    author: Author
    votes: Int
}

input InputPost {
    title: String
    authorId: Int!
}
